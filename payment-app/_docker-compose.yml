version: '3.8'

services:
  # PostgreSQL service
  postgres:
    image: postgres:latest
    container_name: payment-postgres-db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5444:5432"  # Map host port 5444 to container port 5432  # Load PostgreSQL port from .env
    volumes:
      - postgres-data:/var/lib/postgresql/data  # Persistent volume for PostgreSQL data
    networks:
      - payment-network  # Connect to the custom network

  # pgAdmin service
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: payment-pgadmin
    restart: always
    ports:
      - ${PGADMIN_PORT}:80  # Load pgAdmin port from .env
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD}
    networks:
      - payment-network  # Connect to the custom network

  # payment App (NestJS) service
  payment-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: payment-app
    command: sh -c "npm run start:dev"
    ports:
      - "${APP_PORT}:3003"  # Map port 3002 on the host to port 3002 in the container
    environment:
      POSTGRES_URI: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/
    volumes:
      - .:/usr/src/app
    restart: unless-stopped
    networks:
      - payment-network  # Connect to the custom network

volumes:
  postgres-data:  # Volume for PostgreSQL data

networks:
  payment-network:
    driver: bridge  # Custom Docker network
